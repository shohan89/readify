import React from 'react';

const SectionTitle = ({ title }) => {
    return (
        <div className='mt-24 mb-9 text-center'>
            <h3 className='text-4xl font-bold'>{title}</h3>
        </div>
    );
};

export default SectionTitle;